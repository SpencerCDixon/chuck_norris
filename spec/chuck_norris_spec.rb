require 'spec_helper'

describe ChuckNorris do
  it 'has a version number' do
    expect(ChuckNorris::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
